{
  "name": "cherrytech-event-tracking-js-sdk",
  "description": "",
  "version": "0.0.0",
  "private": true,
  "main": "src/main.js",
  "dependencies": {
    "bower": "^1.4.1"
  },
  "devDependencies": {
    "browserify": "^10.2.6",
    "eslint": "^0.24.1"
  },
  "scripts": {
    "build": "mkdir -p dist && browserify src/main.js -o dist/tracking.js",
    "lint": "eslint src/",
    "postinstall": "bower install",
    "prebuild": "npm run lint && npm test",
    "test": "echo \"TODO: no test specified\" && exit 0"
  },
  "engines": {
    "node": ">=0.12.0"
  }
}
